spring:
  profiles:
    active:
      - dev
---
spring:
  profiles: dev
  application:
    name: microservicecloud-config-atguigu-dev #注意格式，有空格
---
spring:
  profiles: test
  application:
    name: microservicecloud-config-atguigu-test